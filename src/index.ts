let base:number=document.getElementById("base");

let exponente:number=document.getElementById("exponente");


let btn = document.getElementById("btn");
btn.addEventListener("click", () => {
  function base(base:number=1) {
  while (base>0) {
    let basepor=(base*base);

    return basepor;
  }
}
 console.log(basepor);

